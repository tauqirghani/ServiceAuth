apiVersion: v1
kind: Service
metadata:
  name: {{ template "apiproject.fullname" . }}
  labels:
    app: {{ template "apiproject.name" . }}
    chart: {{ template "apiproject.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "apiproject.name" . }}
    release: {{ .Release.Name }}
